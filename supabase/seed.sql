-- Local player data is created by the explicit fixture command. The
-- compatibility seed is run by apply-local-player-migrations after the
-- baseline creates its tables and before historical reconciliation migrations.
select 1;
